spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_mybatis?characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: root
  profiles:
    active: dev

logging:
  config: classpath:logback-spring.xml
  level:
    root: info
    cn.alex.mybatis.plus.mapper: debug

# mybatis和mybatis-plus只能配置一个
mybatis-plus:
  # mapper.xml所在位置
  mapper-locations: classpath:mapper/*.xml
  # 实体类添加别名
  type-aliases-package: cn.alex.mybatis.plus.domain
  configuration:
    # 开启支持驼峰命名
    mapUnderscoreToCamelCase: true
    # 配置打印日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      # 设置实体类所对应表统一前缀
      table-prefix:
      # 设置统一的主键生成策略
      id-type: auto
  # 扫描通用枚举包
  type-enums-package: cn.alex.mybatis.plus.enums
