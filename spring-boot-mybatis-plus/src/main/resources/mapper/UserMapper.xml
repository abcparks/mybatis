<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.alex.mybatis.plus.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="cn.alex.mybatis.plus.domain.User">
        <!--@Table `user`-->
        <result column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="age" property="age"/>
        <result column="email" property="email"/>
    </resultMap>

    <sql id="Base_Column_List">
        <!--user表中字段-->
        id, `name`, age, email, create_time, update_time
    </sql>

    <!--auto generated by MybatisCodeHelper on 2021-04-05-->
    <select id="selectByName" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from `user`
        <where>
            <if test="name != null">
                and `name` = #{name}
            </if>
        </where>
    </select>

    <select id="selectPageVO" resultType="cn.alex.mybatis.plus.domain.User">
        select *
        from t_user
        where age = #{age}
    </select>
</mapper>
